import '../../index.scss';

function Title() {
    return (
        <div className="title">
            <h1 className="title__heading">
                Web Developer based in Paris, France |{' '}
                <strong>Node.js / React / SCSS / HTML</strong>
            </h1>
        </div>
    );
}

export default Title;
